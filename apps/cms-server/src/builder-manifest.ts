// El manifiesto del builder es el generado por el sistema de campos (una sola fuente de verdad).
// Reexporta el catálogo demo de @astrocms/schemas (14 bloques) en lugar de duplicarlo aquí.
export { demoBuilderManifest } from "@astrocms/schemas";
